apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: xds
rules:
  - apiGroups:
      - ''
    resources:
      - endpoints
      - services
    verbs:
      - get
      - list
      - watch
